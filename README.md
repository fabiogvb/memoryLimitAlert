# memoryLimitAlert
critical memory usage monitor and alarm

If a memory limit is exceeded, this script in bash/python emits an audible alert and in the system graphical linux interface about critical memory usage.
